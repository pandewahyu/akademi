import React from 'react';
import Calendar from './components/calendar';

const MJadwal = () => {
    return (
        <section>
            <Calendar />
        </section>
    );
};

export default MJadwal;
